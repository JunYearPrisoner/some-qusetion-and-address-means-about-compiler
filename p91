随机产生n个数（0~100），求出它们的最大值，最小值，平均值。
似乎哪里有点问题。

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
	int count=1,sum=0;
	int max=1,min=100;
	int d=10;
	int i=0;
	scanf("%d",&i);
	
	do{
		srand(time(0));
		int number=rand();
		for(;number/d>100;number/d){
			if(number>max){
				max=number;
			}else if(number<min){
				min=number;
			}
		}
		sum=sum+number;
		count++;
	}while(count<i);
	
	
	printf("%d个随机数的最大值为%d,最小值为%d,平均值为%d\n",i,max,min,sum/i);
	
	return 0;
}
